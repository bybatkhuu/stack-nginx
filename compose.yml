services:
  certbot:
    image: bybatkhuu/certbot:3.1.3-260801
    restart: always
    environment:
      TERM: ${TERM:-xterm}
      TZ: ${TZ:-Asia/Seoul}
      CERTBOT_EMAIL: ${CERTBOT_EMAIL}
      CERTBOT_DOMAINS: ${CERTBOT_DOMAINS}
      CERTBOT_DNS_TIMEOUT: ${CERTBOT_DNS_TIMEOUT:-30}
    env_file:
      - path: .env
        required: false
    volumes:
      - "./volumes/storage/nginx/www/.well-known/acme-challenge:/var/www/.well-known/acme-challenge"
      - "./volumes/storage/nginx/ssl:/etc/letsencrypt"
    network_mode: host
    tty: true

  nginx:
    image: bybatkhuu/nginx:3.3.1-260802
    depends_on:
      - certbot
    restart: always
    environment:
      TERM: ${TERM:-xterm}
      TZ: ${TZ:-Asia/Seoul}
    env_file:
      - path: .env
        required: false
    volumes:
      - "./volumes/configs/nginx/templates:/etc/nginx/templates"
      - "./volumes/configs/nginx/sites-enabled:/etc/nginx/sites-enabled"
      - "./volumes/storage/nginx/logs:/var/log/nginx"
      - "./volumes/storage/nginx/www:/var/www"
      - "./volumes/storage/nginx/ssl:/etc/nginx/ssl"
    network_mode: host
    command: ["--https=lets"]
    tty: true
